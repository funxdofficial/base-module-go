# {{README_TITLE}}

Go REST service scaffold generated by [base-module-go](https://github.com/funxdofficial/base-module-go) (`base-module-go --pkg={{PKG_NAME}} --type=rest`).

Echo HTTP server with dummy in-memory CRUD for `item`. No scheduler or cron jobs.

## Architecture

```
main.go
  ├── config.GetConfig()            # env-only config
  ├── module.Init(cfg)              # syslog bootstrap
  └── container.InitService(mod)
        └── service.Run()           # HTTP REST server
              ├── route             # Tier 1 — Echo endpoints
              ├── controller        # Tier 2 — HTTP handlers
              ├── usecase           # Tier 2 — business logic
              └── repository        # Tier 2 — data access
                    ├── model
                    ├── model/request
                    └── model/response
```

## Project structure

```
{{PKG_NAME}}/
├── config/                         # Tier 1 — env-only config
├── container/                      # DI wiring
├── module/                         # syslog bootstrap
├── model/                          # Tier 1 — domain entities
│   ├── request/                    # Tier 2 — inbound DTOs
│   └── response/                   # Tier 2 — outbound DTOs
├── route/                          # Tier 1 — Echo route registration
├── service/                        # Tier 1 — transport entrypoint
│   ├── middleware.go
│   ├── controller/                 # Tier 2
│   ├── usecase/                    # Tier 2
│   └── repository/                 # Tier 2 — in-memory dummy
└── pkg/helpers/
```

## Tier architecture

| Tier | Packages | Role |
|------|----------|------|
| **Tier 1** | `config`, `model`, `route`, `service`, `module`, `container` | Env config, domain entities, HTTP transport, DI |
| **Tier 2** | `model/request`, `model/response`, `service/controller`, `service/usecase`, `service/repository` | DTOs and layered handlers |

**Wiring:** `repository → usecase → controller → route → service (HTTP)`

## Interface file naming

| Layer | Interface file | Implementation file |
|-------|----------------|---------------------|
| Repository | `repository/repository.go` | `repository/repository_<entity>.go` |
| Usecase | `usecase/usecase.go` | `usecase/usecase_<entity>.go` |
| Controller | `controller/controller.go` | `controller/controller_<entity>.go` |

Interface files hold the `*InterFace` type and shared errors. Implementation files hold the concrete struct and entity methods.

## Quick start

```bash
cd {{PKG_NAME}}
cp .env.example .env
go mod tidy
go test ./...
go run .
```

## REST endpoints (dummy CRUD)

| Method | Path | Description |
|--------|------|-------------|
| `GET` | `/health` | Health check |
| `GET` | `/api/v1/items` | List items |
| `POST` | `/api/v1/items` | Create item |
| `GET` | `/api/v1/items/:id` | Get item by ID |
| `PUT` | `/api/v1/items/:id` | Update item |
| `DELETE` | `/api/v1/items/:id` | Delete item |

```bash
curl http://localhost:8080/health
curl http://localhost:8080/api/v1/items
curl -X POST http://localhost:8080/api/v1/items \
  -H 'Content-Type: application/json' \
  -d '{"name":"Item A","description":"dummy item"}'
curl http://localhost:8080/api/v1/items/item-1
curl -X PUT http://localhost:8080/api/v1/items/item-1 \
  -H 'Content-Type: application/json' \
  -d '{"name":"Updated Item","description":"updated"}'
curl -X DELETE http://localhost:8080/api/v1/items/item-1
```

## Configuration (.env)

Configuration is **env-only** — there is no YAML config file.

```bash
cp .env.example .env
```

| Variable | Default | Description |
|----------|---------|-------------|
| `SERVICE_NAME` | `{{PKG_NAME}}` | Service identifier |
| `APP_TIMEZONE` | `Asia/Jakarta` | Application timezone |
| `HTTP_ENABLED` | `true` | Enable REST server |
| `HTTP_PORT` | `8080` | HTTP listen port |
| `LOG_TYPE` | `console` | `console`, `file`, or `all` |
| `LOG_FILE` | `logs/app.log` | Log file path (when using file logging) |
| `LOG_BUFFER_SIZE` | `1000` | Async log buffer size |

## How to add a new entity

Copy the `item` scaffold across layers:

1. Add `model/<entity>.go`, `model/request/<entity>.go`, `model/response/<entity>.go`.
2. Add interface methods in `service/repository/repository.go`; implement in `repository_<entity>.go`.
3. Add interface methods in `service/usecase/usecase.go`; implement in `usecase_<entity>.go`.
4. Add interface methods in `service/controller/controller.go`; implement in `controller_<entity>.go`.
5. Add `route/<entity>.go` and register endpoints in `route/route.go`.
6. Wire the new chain in `container/container.go`.

## Makefile

```bash
make tidy
make test
make build
make run
make run-env    # copy .env.example if missing, then go run .
```
